# Steps to get started

install modules

```bash
pnpm i
```

make an env file and add your local info

```bash
echo -e "PORT=\nDB_HOST=\nDB_USER=\nDB_PASSWORD=\nDB_NAME=" > .env
```

run locally

```bash
pnpm run dev
```
